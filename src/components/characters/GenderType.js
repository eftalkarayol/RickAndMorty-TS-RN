import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const GenderType = () => {
  return (
    <View>
      <Text>GenderType</Text>
    </View>
  );
};

export default GenderType;

const styles = StyleSheet.create({});
